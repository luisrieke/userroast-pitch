[project]
name = "hamburg-hackathon-backend"
version = "0.1.0"
description = "FastAPI backend for the Hamburg Hackathon app"
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]",
    "modal",
    "python-dotenv",
]
